project.ext {
    submodules = ['database', 'auth', 'storage', 'firestore', 'common']
    group = 'com.firebaseui'
    version = '3.1.0'
    pomdesc = 'Firebase UI Android'

    compileSdk = 26
    targetSdk = 26
    minSdk = 14

    buildTools = '26.0.2' // Remember to also update in .travis.yml

    firebaseVersion = '11.6.0'
    supportLibraryVersion = '27.0.0'
    architectureVersion = '1.0.0-beta1'
}
